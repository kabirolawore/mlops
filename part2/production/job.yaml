$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
code: .
command: >-
  python train_model.py 
  --trainingdata ${{ inputs.training_data }}
inputs:
  training_data:
    type: uri_file
    path: azureml:iris:1
environment: azureml://registries/azureml/environments/sklearn-1.1/versions/14
compute: practical-ml-compute
experiment_name: prac_demo
description: Practical mlops demo - training model on iris dataset


# parser = argparse.ArgumentParser()
# parser.add_argument("--trainingdata", type=str, required=True, help='Dataset for training')
# args = parser.parse_args()